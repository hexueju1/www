'use strict';

import React, { PureComponent, Component } from 'react';
import {
  StyleSheet,
  TouchableOpacity,
  TouchableHighlight,
  View,
  Image
} from 'react-native';
import { Root, ActionSheet, Accordion, Tab, Tabs, Container, Header, Content, Button, Text, Form, Item, Input, Label, Picker, Icon } from 'native-base';

class GButton extends Component {

  render() {
    return (
      <View style={{ alignItems: 'center' }}>

      </View>
    );
  }
}

module.exports = GButton;
